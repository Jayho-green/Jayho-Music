# AI Agent核心概念快速参考

## 🎯 一句话理解

- **User Prompt**: 你问AI的话
- **System Prompt**: AI的"人设设定"
- **Agent**: 连接你、AI和工具的智能协调者
- **Function Calling**: AI调用工具的标准化方式
- **MCP**: Agent和工具服务的"USB协议"

---

## 🔄 完整流程（5步）

```
1. 用户提问
   ↓
2. Agent获取工具信息（通过MCP）
   ↓
3. Agent发送请求给AI（包含Prompt和工具信息）
   ↓
4. AI返回工具调用请求
   ↓
5. Agent执行工具 → 返回结果给AI → AI生成答案 → 返回用户
```

---

## 📊 概念对比表

| 特性 | System Prompt | Function Calling |
|------|---------------|------------------|
| **格式** | 自然语言描述 | JSON标准化格式 |
| **位置** | 混在对话中 | 独立字段 |
| **可靠性** | 需要重试 | 服务器自动处理 |
| **兼容性** | 所有模型支持 | 部分模型支持 |
| **标准化** | 不统一 | 相对统一 |

---

## 🏗️ 架构三要素

```
┌─────────────┐
│   用户      │
└──────┬──────┘
       │
       ▼
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   Agent     │◄────►│  AI模型     │◄────►│  MCP Server │
│ (协调者)    │      │ (GPT等)     │      │  (工具库)   │
└─────────────┘      └─────────────┘      └─────────────┘
```

---

## 💡 关键记忆点

1. **Prompt分两种**：User（你说的话）+ System（AI的人设）
2. **Agent是桥梁**：连接用户、AI和工具
3. **Function Calling更可靠**：标准化格式，自动重试
4. **MCP是协议**：不是AI模型，只是工具管理协议
5. **它们协作工作**：不是取代，而是配合

---

## 🚀 技术演进

```
基础聊天 → 人设设定 → 工具调用 → 标准化 → 服务化
  (2023)    (System)   (Agent)   (Function)  (MCP)
```

---

## ❓ 常见问题

**Q: Agent是智能体，那智能体又是什么？**  
A: Agent就是负责协调AI模型、工具和用户的程序，可以理解为"智能助手"。

**Q: MCP是AI时代的USB协议，可以接U盘吗？**  
A: MCP是通信协议，不是物理接口。它规范的是Agent和工具服务之间的通信方式，不能接物理U盘，但可以提供类似文件读写的服务（Resources）。

**Q: Function Calling和System Prompt哪个更好？**  
A: Function Calling更标准化、更可靠，但System Prompt兼容性更好。目前两者并存，根据场景选择。

---

## 📝 实际例子

**场景**: 让AI帮你找文件

```
用户: "帮我找原神的安装目录"

Agent流程:
1. 通过MCP获取文件操作工具（list_files, read_files）
2. 将工具信息发送给AI（Function Calling格式）
3. AI返回: 调用list_files("/")
4. Agent执行list_files，返回目录列表
5. AI分析结果，继续调用read_files或list_files
6. 循环直到找到目标
7. 返回用户: "原神安装在 C:\Program Files\Genshin Impact"
```

---

**快速记忆口诀**：
- **Prompt** = 提示词（用户说 + 系统设）
- **Agent** = 协调者（连接一切）
- **Function** = 标准化（工具调用）
- **MCP** = 协议（工具管理）
